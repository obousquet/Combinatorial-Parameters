{
  "id": 38,
  "name": "Labeled Sample Compression",
  "symbol": "\\mathrm{LSC}",
  "category": "compression",
  "symmetric": true,
  "monotonic": true,
  "p_monotonic": true,
  "c_monotonic": true,
  "doubly_monotonic": true,
  "strictly_monotonic": null,
  "definition": "The **Labeled Sample Compression** of a nonempty finite binary class $\\mathcal H\\subseteq\\{0,1\\}^{\\mathcal X}$ is the smallest integer $k\\ge0$ for which there are fixed maps $\\kappa$ and $\\rho$ with the following property. For every realizable labeled sample $(S,y)$, meaning $S\\subseteq\\mathcal X$ and $y=h|_S$ for some $h\\in\\mathcal H$, the encoder retains $\\kappa(S,y)=(U,y|_U)$ with $U\\subseteq S$ and $|U|\\le k$, and the decoder returns $\\rho(U,y|_U)\\in\\{0,1\\}^{\\mathcal X}$ agreeing with $y$ on all of $S$. The decoder receives only the retained labeled subset: no auxiliary message, ordering, or repeated examples are allowed. Reconstruction may be outside $\\mathcal H$.",
  "graph_summary": "Smallest labeled subset allowing consistent reconstruction, with no auxiliary message. Reconstruction may be improper.",
  "comments": "Some literature counts retained examples plus auxiliary bits as compression size. The exponential theorem in \\cite{moran2016compression} uses such messages. A separate survey proposition (prop:compression-run-message-removal) converts kernel size $k$ and $m$ possible messages to message-free labeled size at most $k+2(k+1)(k+6+2\\lceil\\log_2m\\rceil)$. Few label runs are reconstructed directly; otherwise actual observed labels encode a self-locating message beside the original kernel. The cost is quadratic in the total kernel-plus-bit budget, and reconstruction may be improper or differ from the original output outside the sample. Combining this conversion with the source theorem gives $\\mathrm{LSC}\\le 2^{O(\\mathrm{VC})}$ for positive VC dimension, and value zero at VC dimension zero (cor:message-free-vc-finite). The older ordered-gap conversion (prop:compression-message-removal) preserves properness when available but costs $O(m(k+1)^3)$. The linear-VC question remains open. Full proofs are survey-owned; the message-removal argument is not attributed to the cited paper. No stable or proper VC-bound consequence is asserted.",
  "origin_references": "The standard sample-compression framework is developed in \\cite{FW1995}.",
  "short_name": "labeled_sample_compression",
  "monotonicity_evidence": {
    "monotonic": {
      "proof": "Restrict a possibly improper labeled compression scheme for $\\mathcal H$ to samples realizable by $\\mathcal G\\subseteq\\mathcal H$.  Its reconstruction remains consistent, with the same compression bound.",
      "kind": "direct_argument"
    },
    "p_monotonic": {
      "proof": "Regard a labeled sample on retained coordinates as a labeled sample on the original domain, apply the scheme for $\\mathcal H$, and restrict the reconstruction.  Any projected-realizable sample has an extension in $\\mathcal H$, so the restricted reconstruction is consistent and uses no more retained labeled examples.",
      "kind": "direct_argument"
    },
    "doubly_monotonic": {
      "proof": "By definition, being monotonic under both subfamilies and projections is doubly monotonic.",
      "kind": "definitional_consequence"
    },
    "c_monotonic": {
      "proof": "A conditioning is restriction to a subfamily followed by a projection; hence doubly monotonicity implies c-monotonicity.",
      "kind": "definitional_consequence"
    }
  }
}
